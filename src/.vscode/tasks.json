{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "cd build && cmake --build ./ --config Debug --target Editor -- -j 16",
            "problemMatcher": [
                "$gcc"
            ]
        },
        {
            "label": "make Game",
            "type": "shell",
            "command": "cd build && cmake --build ./ --config Debug --target Game -- -j 16",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}