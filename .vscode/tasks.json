{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "cd build && cmake --build ./ --config Debug -- -j 16",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "make Game",
            "type": "shell",
            "command": "cd build && cmake --build ./ --config Debug --target Game -- -j 16",
            "problemMatcher": [
                "$gcc"
            ]
        }        
    ]
}